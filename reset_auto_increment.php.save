<?php
require_once '../db.php';

// delete all records
$delete = mysqli_query($conn, "DELETE FROM phonebook");

// reset auto-increment to 1
$reset = mysqli_query($conn, "ALTER TABLE phonebook AUTO_INCREMENT = 1");

if ($delete && $reset) {
    echo json_encode(["success" => true, "message" => "Database cleared and reset to ID 1"]);
} else {
    echo json_encode(["success" => false, "error" => mysqli_error($conn)]);
}

mysqli_close($conn);
?>
